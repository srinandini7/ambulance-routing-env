name: ambulance-routing-env
version: 1.0
description: Emergency ambulance routing simulation

observation: Observation
action: Action
reward: float

endpoints:
  reset: /reset
  step: /step
  state: /state